{
    "routes": [
        {
            "method": "POST",
            "path": "/greetings",
            "handler": "notification.send_greetings",
            "config": {
              "policies": []
            }
        }
    ]
}